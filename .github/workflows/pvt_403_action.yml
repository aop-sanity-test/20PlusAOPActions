name: One failing test

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: aop-sanity-test/mu-js-action-pri-int@1.0.0
        with:
          milliseconds : 1
